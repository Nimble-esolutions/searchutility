version: '3.8'

services:
  db:
    image: postgres:15
    volumes:
      - postgres_data:/var/lib/postgresql/data/
    environment:
      POSTGRES_DB: flowdocs
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

  web:
    image: myrepo/myapp:2.0
    build: .
    command: >
      sh -c "
        python flowdocs/manage.py migrate &&
        python flowdocs/manage.py collectstatic --noinput &&
        gunicorn flowdocs.wsgi:application --bind 0.0.0.0:8000 --workers=3
      "
    volumes:
      - static_volume:/app/staticfiles
      - media_volume:/app/media
    ports:
      - "8000:8000"
    environment:
      - DEBUG=False
      - DB_NAME=flowdocs
      - DB_USER=postgres
      - DB_PASSWORD=postgres
      - DB_HOST=db
      - DB_PORT=5432
      - REDIS_URL=redis://redis:6379/1
      - DJANGO_SETTINGS_MODULE=flowdocs.settings
    depends_on:
      - db
      - redis

volumes:
  postgres_data:
  static_volume:
  media_volume:
